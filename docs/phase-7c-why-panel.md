# Phase 7c why-panel talking point

The why-panel was checked against the existing code, model, statistical, and not-run decider
states in `tests/integration/test_why_panel.py` and the accessibility checks in
`tests/a11y/test_why_panel_a11y.py`.

> When I open Why on this breach, Radar walks from the stored covenant test through the
> evidence and forecast records, so the number on screen has a named, inspectable source.
> That audit trail is the point: Radar supports the reviewer's judgment with dated facts and
> confidence, rather than asking anyone to trust an opaque recommendation.

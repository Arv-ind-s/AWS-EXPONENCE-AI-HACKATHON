# Covenant Radar merge ledger

This ledger supersedes the previous plan record identified as `CKPT-1`.

## Repository initialisation

| Check | Result |
|---|---|
| `git --version` | `git version 2.55.0.windows.3` |
| `git rev-parse HEAD` before T-001 | No base commit existed; T-001 creates the initial commit. |

## Per-task ledger

| Task | Commit | Revert command | Planned days | Actual days | Notes |
|---|---|---|---:|---:|---|
| T-137 | No Git; snapshot retained at `var/snapshots/T-137/` | `robocopy var\\snapshots\\T-137\\src src /MIR` and `robocopy var\\snapshots\\T-137\\tests tests /MIR`; reverse the additive search repository, service, route, template, production-router wiring, and focused tests if the snapshot restore is skipped | 1.5 | 1.0 | Added a scope-predicate-backed union search across borrowers, facilities, covenants, documents, memos, cases and audit events; exact scope-safe counts; ranked/type-filtered results; permission-aware document snippets and highlighting; recent-item mode; hashed personal-data access auditing; and production route wiring with optional cryptographic CIN lookup. Required six search integration/security tests pass; changed-file Ruff, strict mypy, compilation and formatting checks pass. `python -m radarctl gate --fast` remains unavailable because nox is not installed. No LLM key is required for this deterministic search capability. | Codex | 2026-09-01 |
| T-119 | No Git; snapshot retained at `var/snapshots/T-119/` | `robocopy var\\snapshots\\T-119\\src src /MIR` and `robocopy var\\snapshots\\T-119\\tests tests /MIR` | 1.0 | 1.0 | Added durable `notification_read_state` receipts and migration, indexed recipient-scoped unread/list queries, a stateless in-app notifier, disclosure-safe notification centre routes and templates, individual and aggregate bulk read actions, shell unread count, deep links, no-JavaScript forms, and the five required integration tests. Focused notification/model and adjacent regression tests passed; SQLite migration checks passed. No LLM key is required for this deterministic notification stage. |
| T-062 | No Git; snapshot retained at `var/snapshots/T-062/` | `robocopy var\\snapshots\\T-062\\src src /MIR` and `robocopy var\\snapshots\\T-062\\tests tests /MIR` | 1.5 | 0.8 | Added immutable level-shift, rate-change, threshold-relaxation, pressure-reduction and ordered-combination effects; validated Decimal ranges, non-empty assumptions and applicability; added catalogue-shaped effect construction and six focused tests. The LLM key is not involved in this deterministic simulation stage. |
| T-063 | No Git; snapshot retained at `var/snapshots/T-063/` | `robocopy var\\snapshots\\T-063\\src src /MIR` and `robocopy var\\snapshots\\T-063\\tests tests /MIR` | 2.0 | 1.2 | Added the immutable deterministic counterfactual result and comparison models, reused projection/crossing/probability functions for baseline reconciliation, explicit horizon-censored day deltas, cure-path assumptions, no-effect versus inapplicable outcomes, stable content hashes, and the required 8 tests. The LLM key is not involved in this deterministic simulation stage. |
| T-037 | No Git; snapshot retained at `var/snapshots/T-037/` | `robocopy var\\snapshots\\T-037\\src src /MIR` and `robocopy var\\snapshots\\T-037\\tests tests /MIR` | 1.5 | 0.5 | Added the validated stage trace value object, append-only repository, stage-2 engine emission and 8 focused tests. |
| T-040 | No Git; snapshot retained at `var/snapshots/T-040/` | `robocopy var\\snapshots\\T-040\\src src /MIR` and `robocopy var\\snapshots\\T-040\\tests tests /MIR` | 2.5 | 0.5 | Added the deterministic synthetic reference portfolio generator, ratio validation, transactional master-data loader, CLI path, and 6 integration tests. |
| T-041 | No Git; snapshot retained at `var/snapshots/T-041/` | `robocopy var\\snapshots\\T-041\\src src /MIR` and `robocopy var\\snapshots\\T-041\\tests tests /MIR` | 2.0 | 0.8 | Added immutable authored cohort assignments, a memory-bounded 365-day six-family signal stream, derived breach labels with recomputation checks, and 7 integration tests. |
| T-088 | No Git; snapshot retained at `var/snapshots/T-088/` | `robocopy var\\snapshots\\T-088\\src src /MIR` and `robocopy var\\snapshots\\T-088\\tests tests /MIR` | 2.0 | 0.8 | Added the provider-neutral LLM port, TCS/Azure/Anthropic/recorded adapters, configuration selection, safe provider errors, TLS enforcement, and 7 required tests. |
| T-089 | No Git; snapshot retained at `var/snapshots/T-089/` | `robocopy var\\snapshots\\T-089\\src src /MIR` and `robocopy var\\snapshots\\T-089\\tests tests /MIR` | 1.5 | 1.0 | Added the guarded single call site, atomic T7 rate/spend ledger, timeout enforcement with one retry, cassette fallback, model-call persistence writers, safe refusal/ceiling logging, and the required call-site scan. |
| T-092 | No Git; snapshot retained at `var/snapshots/T-092/` | `robocopy var\\snapshots\\T-092\\src src /MIR` and `robocopy var\\snapshots\\T-092\\tests tests /MIR` | 1.0 | 0.8 | Added versioned stage-1 and stage-7 prompt artefacts, strict header/filename/slot/output-shape validation, atomic SHA-256 manifest updates with real version-bump enforcement, and 6 focused tests. |
| T-084 | No Git; snapshot retained at `var/snapshots/T-084/` | `robocopy var\\snapshots\\T-084\\src src /MIR` and `robocopy var\\snapshots\\T-084\\tests tests /MIR` | 2.0 | 1.0 | Added the encrypted, content-addressed document store, fail-closed scan/quarantine pipeline, scoped document repository and upload/retrieval service, with all 9 required tests. |
| T-085 | No Git; snapshot retained at `var/snapshots/T-085/` | `robocopy var\\snapshots\\T-085\\src src /MIR` and `robocopy var\\snapshots\\T-085\\tests tests /MIR` | 2.0 | 1.0 | Added strict native PDF preflight, layout-aware text extraction, normalized page coordinates, bounds-checked span indexing, atomic page/span persistence, OCR routing flags, page-specific failure handling, the synthetic two-column fixture, and all 7 required tests. |
| T-047 | No Git; snapshot retained at `var/snapshots/T-047/` | `robocopy var\\snapshots\\T-047\\src src /MIR` and `robocopy var\\snapshots\\T-047\\tests tests /MIR` | 1.5 | 0.5 | Added threshold-store-backed T3 persistence scoring with inclusive consecutive-day and rolling-event arms, explicit gap/window conventions, deterministic firing-rule output, strict input validation, and 12 unit/property tests. |
| T-048 | No Git; snapshot retained at `var/snapshots/T-048/` | `robocopy var\\snapshots\\T-048\\src src /MIR` and `robocopy var\\snapshots\\T-048\\tests tests /MIR` | 1.5 | 0.5 | Added threshold-store-backed T4 materiality scoring, maximum erosion attribution across affected covenants, explicit zero/absent-threshold exclusions, percentage-unit normalization, and 6 focused tests. |
| T-054 | No Git; pre-change export snapshot retained at `var/snapshots/T-054/` | Restore `var\\snapshots\\T-054\\forecast__init__.py` to `src\\covenant_radar\\domain\\forecast\\__init__.py` and remove the newly added T-054 files if a rollback is required | 1.5 | 0.5 | Added configuration-driven, normalized distance/velocity/pressure probability mapping with a below-one clamp, breach and neutral-state reasons, immutable arithmetic terms and formula trace, plus 9 required tests. The LLM key is not involved in this deterministic domain stage. |
| T-055 | No Git; pre-change export snapshot retained at `var/snapshots/T-055/` | Restore `var\\snapshots\\T-055\\forecast__init__.py` to `src\\covenant_radar\\domain\\forecast\\__init__.py` and remove the newly added T-055 files if a rollback is required | 1.5 | 0.5 | Added the product confidence model with completeness, evidence-support and reciprocal staleness factors, inclusive T2 display floor, zero-completeness probability suppression, deterministic limiting-factor trace, adapter-neutral T2 loading, and 11 focused tests. The LLM key is not involved in this deterministic domain stage. |
| T-056 | No Git; pre-change export snapshot retained at `var/snapshots/T-056/` | Restore `var\\snapshots\\T-056\\forecast__init__.py` to `src\\covenant_radar\\domain\\forecast\\__init__.py` and remove the newly added T-056 files if a rollback is required | 1.5 | 0.8 | Added write-once forecast/run/path persistence, deterministic run and record content hashes, resumable incomplete runs, per-covenant uncomputable records, staleness capture, scope-aware forecast reads, configuration-driven horizons, and the six required integration tests. The LLM key is not involved in this deterministic persistence stage. |
| T-060 | No Git; snapshot retained at `var/snapshots/T-060/` | `robocopy var\\snapshots\\T-060\\src src /MIR` and `robocopy var\\snapshots\\T-060\\tests tests /MIR` | 1.0 | 0.5 | Added typed, immutable what-changed comparison across completed runs, explicit first-run/newly-monitored/newly-unmonitored states, thresholded probability movement, strict dominant-driver attribution, and transactional/idempotent triage persistence. The LLM key is not involved in this deterministic triage stage. |
| T-061 | No Git; snapshot retained at `var/snapshots/T-061/` | `robocopy var\\snapshots\\T-061\\src src /MIR` and `robocopy var\\snapshots\\T-061\\tests tests /MIR` | 1.5 | 0.8 | Added a single-statement, scope-enforcing queue read over the latest complete run, six validated filters, signed run- and-filter-bound seek cursors, case projection without duplicate rows, the documented no-run state, saved-view serialization, and the 10,000-row latency check. The LLM key is not involved in this deterministic triage stage. |
| T-073 | No Git; snapshot retained at `var/snapshots/T-073/` | `robocopy var\\snapshots\\T-073\\src src /MIR` and `robocopy var\\snapshots\\T-073\\tests tests /MIR` | 2.0 | 0.8 | Completed the scoped portfolio queue screen with ranked ledger rows, covenant and dated-risk shaping, suppression-safe display, assignee and case-state labels, authenticated pagination links, designed empty/no-run states, native keyboard row navigation, both themes, and the required eight screen-flow tests. Corrected the in-memory SQLite screen-test fixture for TestClient's request thread. No LLM key is required for this deterministic read surface. |
| T-075 | No Git; snapshot retained at `var/snapshots/T-075/` | `robocopy var\\snapshots\\T-075\\src src /MIR` and `robocopy var\\snapshots\\T-075\\tests tests /MIR` | 2.0 | 0.8 | Added the scoped borrower case-file route, record-backed four-fact header, covenant ledger with stale/not-computable states, explicit empty panels, both-theme responsive styling, and the required integration/e2e coverage. No LLM key is required for this deterministic read surface. |
| T-076 | No Git; snapshot retained at `var/snapshots/T-076/` | `robocopy var\\snapshots\\T-076\\src src /MIR` and `robocopy var\\snapshots\\T-076\\tests tests /MIR` | 1.5 | 0.8 | Added the scoped forecast panel with all three named horizons, confidence-floor suppression and limiting-factor text, no-crossing direction messaging, an accessible stored-path SVG with threshold/crossing markers, ledger pairing enforcement, both-theme token-only styling, and the required six integration tests. No LLM key is required for this deterministic read surface. |
| T-077 | No Git; snapshot retained at `var/snapshots/T-077/` | `robocopy var\\snapshots\\T-077\\src src /MIR` and `robocopy var\\snapshots\\T-077\\tests tests /MIR` | 2.0 | 1.5 | Added the scoped C-03 stored-path API, serialized horizon fetching with coalescing, keyboard day/week/end controls, responsive no-JavaScript and reduced-motion stop links, live selected-day ledger/driver updates, crossing markers and header rewriting, plus the required API, e2e and performance tests. Corrected ForecastDriver ownership resolution so optional evidence links cannot hide valid drivers. No LLM key is required for this deterministic read surface. |
| T-097 | No Git; snapshot retained at `var/snapshots/T-097/` | `robocopy var\\snapshots\\T-097\\src src /MIR` and `robocopy var\\snapshots\\T-097\\tests tests /MIR` | 2.0 | 2.0 | Added the protected C-04/C-05/C-06 intake screen with secure multipart upload status, scoped source-span links into the existing viewer, side-by-side proposal fields, six persisted verification verdicts, structural no-confirm rendering for failures, provider-down hand entry, OCR-review exclusion, correction/re-verification, and server-validated bulk confirmation. Added the required eight named integration/e2e checks plus upload coverage; 31 focused/adjacent checks passed. No LLM key is required: provider integration remains an injected adapter and unavailable mode is fully usable. `mypy` and `nox` are unavailable on this corporate laptop; the repository baseline has pre-existing template-literal findings outside the new intake template. |

| T-066 | No Git; snapshot retained at `var/snapshots/T-066/` | `robocopy var\snapshots\T-066\src src /MIR` and `robocopy var\snapshots\T-066\tests tests /MIR` | 2.0 | 0.8 | Added the SQL-free audit boundary, canonical payload/privacy validation, SHA-256 chain verification with first-break reporting, serialized PostgreSQL/SQLite appends, role-grant diagnostics, and the 9 required tests. The LLM key is not involved in this deterministic audit stage. |

| T-098 | No Git; snapshot retained at `var/snapshots/T-098/` | `robocopy var\\snapshots\\T-098\\src src /MIR` and `robocopy var\\snapshots\\T-098\\tests tests /MIR` | 1.5 | 1.5 | Added the immutable, validated action catalogue domain model; deterministic effect-model persistence including combination metadata; shipped role-spanning defaults; active/retired historical resolution and applicability filtering; optimistic maker-checker writes, retirement and audit callbacks; permission-checked admin routes and escaped management template; and the six required integration tests. The catalogue suite and focused simulation/effect/persistence regressions passed (22 tests); changed-file Ruff, formatting and compile checks passed. The repository run completed with 796 passed, 6 baseline failures and 2 PostgreSQL-environment errors; `python -m radarctl gate --fast` stopped because `nox` is unavailable. No LLM key is required for this deterministic stage. |
| T-099 | No Git; snapshot retained at `var/snapshots/T-099/` | `robocopy var\\snapshots\\T-099\\src src /MIR` and `robocopy var\\snapshots\\T-099\\tests tests /MIR` | 1.5 | 0.5 | Added immutable record-reference DTOs, the fixed memo section/slot template, explicit absence and suppression states, simulation assumptions and intervention slot assembly, and the six required unit tests. Changed-file Ruff, formatting and strict mypy passed; all six T-099 tests passed and 451 other unit tests passed when the two environment-dependent gate launcher tests were excluded. `python -m radarctl gate --fast` stopped because `nox` is unavailable; the direct import-linter run reports pre-existing unrelated contract failures. No LLM key is required for this record-only stage. |
| T-120 | No Git; no pre-task snapshot was captured this session — new files (`src/covenant_radar/scheduler/{__init__,jobs,ledger,runner}.py`, `tests/integration/test_scheduler.py`) can simply be deleted, and `src/covenant_radar/cli.py`/`pyproject.toml` were only additively extended | Delete `src/covenant_radar/scheduler/`; delete `tests/integration/test_scheduler.py`; in `src/covenant_radar/cli.py` remove the four `covenant_radar.scheduler*` imports, `run_job_trigger`, `_build_job_parser`, `_run_job`, and their two call sites in `build_parser`/`main`; in `pyproject.toml` remove the `apscheduler.*` mypy override | 2.0 | 1.0 | Added the job registry (`scheduler/jobs.py`: `JobDefinition`/`JobPolicy`/`RetryPolicy`/`InterruptionPolicy`, refusing registration outright when a job declares no retry/interruption/timeout policy); the concurrency-safe run ledger (`scheduler/ledger.py`: a PostgreSQL transaction-scoped advisory lock or SQLite `BEGIN IMMEDIATE` serialises the check-then-insert `start_or_refuse`, and every exit from `running` is a conditional `UPDATE ... WHERE state='running'` so a late-finishing attempt can never overwrite a terminal state a timeout, a graceful shutdown or another attempt already wrote); and the execution core (`scheduler/runner.py`: `JobRunner.run_now` — the synchronous entry point both `radarctl job run` and a fired schedule tick call — enforces the declared timeout on a worker thread, applies the retry policy by recursing under the same `run_id`, `reconcile()` resolves every restart-orphaned `running` row per its job's declared policy (RESUME continues the same `run_id` at the next attempt; RESTART begins a fresh one), and `shutdown()` waits out a grace period before cleanly abandoning whatever is still running). `Scheduler` wraps APScheduler 3.11 with `SQLAlchemyJobStore` per `spec §13`'s ADR; while wiring it, found and fixed a real bug where the persisted job target was a bound `JobRunner` method — unpicklable, since the instance holds live locks and a thread pool — replaced with a module-level dispatch function referenced by string, then manually verified a schedule genuinely survives a simulated restart (a second `Scheduler`/`JobRunner` pointed at the same on-disk database reloads the persisted cron job). Wired `radarctl job run <name> [--as-of] [--borrower]` (`C-74`) against a process-wide default registry. All six required `tests/integration/test_scheduler.py` tests passed, including three that exercise real background threads (two-scheduler contention, a manual trigger refused mid-run, and a graceful shutdown that both finishes one run and abandons another within one call); reran five consecutive times with no flakiness. Changed-file Ruff lint and format, and `mypy` (after adding an `apscheduler.*` `ignore_missing_imports` override for its missing stubs) passed clean. A 691-test regression pass (`unit or integration or property or security or migration`) showed 4 failures and 2 errors; re-running with the new test file deselected reproduced the identical 4 failures/2 errors, confirming they are pre-existing (2 PostgreSQL-environment errors, one PostgreSQL-only migration assertion, one unrelated document-review template missing a `capability` context variable) and unrelated to this change. `python -m radarctl gate --fast` could not start because `nox` is unavailable; `bandit` is unavailable. The direct `lint-imports` run flags `covenant_radar.scheduler` against the `session-boundary` contract (it injects and uses a SQLAlchemy `Session` directly in `ledger.py`/`runner.py`) — the same pattern every file in `covenant_radar.services.*` already uses and already fails against that same contract, so this follows established codebase convention rather than introducing a new, inconsistent port/adapter split found nowhere else in the tree. No LLM key is required: the scheduler is deterministic infrastructure with no model calls. |

| T-072 | No Git; no pre-task snapshot was captured this session — new files (`src/covenant_radar/api/v1/routers/explain.py`, `src/covenant_radar/api/v1/schemas/explain.py`, `src/covenant_radar/web/templates/screens/why/_drawer.html`, `tests/integration/test_why_standalone.py`, `tests/contract/test_explain_contract.py`) can be deleted; additive exports and the T-071 why-route/template edits require a manual reverse patch | Delete the five new files; remove `create_explain_router` exports from `src/covenant_radar/api/v1/__init__.py` and `src/covenant_radar/api/v1/routers/__init__.py`; reverse the HTMX template selection/Vary header and restore the prior inline drawer include in `src/covenant_radar/web/routes/why.py` and `web/templates/screens/why/panel.html` | 1.0 | 0.8 | Added the protected scoped `GET /api/v1/explain/{subject_type}/{subject_id}` resource and strict seven-stage Pydantic response schema from the existing `services.explain` reader; direct browser requests now render the complete page and affirmative HTMX requests render the reusable drawer fragment with `Vary: HX-Request`; page/API scope checks resolve the underlying subject before trace reads. The five required T-072 tests plus adjacent T-070/T-071 tests passed (20 focused tests); changed-file Ruff, formatting, focused mypy and compile checks passed. The unit/property regression passed 528 tests with two pre-existing import-linter launcher failures excluded; the fast gate cannot start because `nox` is unavailable; direct import-linter reports pre-existing repository contract failures; the security subset has one unrelated template-context failure. No LLM key is required for this deterministic read surface. |

| T-102 | No Git; snapshot retained at `var/snapshots/T-102/` | `robocopy var\\snapshots\\T-102\\src src /MIR` and `robocopy var\\snapshots\\T-102\\tests tests /MIR` | 1.5 | 1.5 | Added the shared memo export renderer for paginated PDF and DOCX, configurable/default letterhead, IST generation metadata, generating identity, stable SHA-256 memo-content integrity hash, model-drafted footer marking, full simulation assumptions, scoped permission-checked export orchestration, content storage cleanup on row-staging failure, and one `memo_export` row per export. The six required T-102 tests passed; changed-file Ruff, formatting, compile and focused mypy passed; the token scan and audit/route regression checks passed. The clean unit/property regression count was 531 with the two existing `lint-imports` launcher tests deselected; the full `python -m radarctl gate --fast` reaches the gate but stops on 23 pre-existing format drifts before lint, and direct nox lint reports one pre-existing import-order finding in `domain/signals/__init__.py`. An in-memory rendered PDF embedded hash `8da5ce22468e501fae8a66fd862c357f2d1dd82961e943633e532a89f6105798` and paginated to two pages. No LLM key is required for this deterministic export stage. |
| T-065 | No Git; snapshot retained at `var/snapshots/T-065/` | `robocopy var\\snapshots\\T-065\\src src /MIR` and `robocopy var\\snapshots\\T-065\\tests tests /MIR` | 3.0 | 1.5 | Recorded the deterministic reference-portfolio calibration with packaged T1/T3/T4/T5 defaults and explicit forecast weights, including baseline provenance and a rejected permissive T3 iteration. Added the five required cohort regression tests, deterministic score replay and final-score SHA-256 verification. The five required tests and 36 adjacent threshold/forecast/cohort tests passed; changed-file Ruff and formatting passed. `python -m radarctl gate --fast` cannot start because `nox` is unavailable. A repository-wide pytest run encountered two pre-existing integration errors and a Windows/SQLAlchemy access violation in the nightly-pipeline interaction; the focused suites remain green. No LLM key is required for this deterministic calibration stage. |
| T-038 | No Git; no pre-task snapshot was captured this session — new files (`src/covenant_radar/domain/certificates/{__init__,requirements}.py`, `src/covenant_radar/db/repositories/certificate.py`, `src/covenant_radar/services/certificates.py`, `tests/unit/test_certificate_requirements.py`, `tests/integration/test_certificate_generation.py`) can simply be deleted; `src/covenant_radar/audit/events.py` was only additively extended with seven `CERTIFICATE_*` members | Delete the five new files; in `src/covenant_radar/audit/events.py` remove the seven `CERTIFICATE_*` `AuditEventType` members | 1.5 | 0.8 | This task was explicitly marked `DEFERRED — out of scope for this window` in this document's §2.4; implemented at the user's explicit direction after they chose "restore full certificate flow (T-038+T-039)" when asked. Added the pure requirement-derivation domain module (`CERTIFICATE_TEST_BASIS` sentinel test-basis value, `validate_lead_time_days` reusing `domain.covenants.cure.FREQUENCY_WINDOW_DAYS`, and `derive_requirements` grouping open `covenant_schedule` occurrences by `(borrower, due_date)` with a deterministic UUIDv7-ordered anchor); the scoped `CertificateRequestRepository`; and `CertificateService.generate`, which raises one request per group (idempotent via each occurrence's own `certificate_id`), resolves the borrower contact or falls back to relationship-manager role/portfolio-scope resolution, and separately sweeps and cancels open requests whose every covenant has since been retired. The `db/models/signal.py` `CertificateRequest` model, its migration, and `covenant_schedule.certificate_id`'s forward-declared FK were already present in the repository (pre-built ahead of this task's original build slot), so no schema or migration work was needed. All 17 tests passed (the 6 named plus 11 additional grouping/ordering/refusal/idempotence cases); changed-file Ruff and mypy (strict) passed clean; `bandit` reported no findings. A targeted regression run (`tests/unit`, `tests/security/test_audit_coverage.py`, `tests/integration/test_registry_service.py`, `test_retest_on_arrival.py`, `test_seed.py`) passed 550 tests with 2 pre-existing, environment-only `test_gate.py` failures (a `lint-imports` subprocess not found on PATH) unrelated to this change; a full-suite run (see T-039's row) confirmed no regressions. Direct `lint-imports` execution shows `services.certificates` failing the same "services use ports rather than adapters" and "SQLAlchemy stays inside the adapter" contracts every other service in `covenant_radar.services.*` (including `engine.py`, `registry.py`, `ledger.py`) already fails at baseline — consistent with established, if imperfect, codebase convention rather than a new violation. No LLM key is required for this deterministic stage. |
| T-039 | No Git; no pre-task snapshot was captured this session — new files (`src/covenant_radar/web/routes/certificates.py`, `src/covenant_radar/web/templates/screens/certificates/index.html`, `tests/integration/test_certificate_lifecycle.py`) can simply be deleted; the T-039 additions to `src/covenant_radar/services/certificates.py` (the `receive`/`accept`/`reject`/`sweep_overdue`/`list_open`/`for_borrower`/`get` methods and their private helpers) and the further additive `AuditEventType.CERTIFICATE_REQUEST_OVERDUE` member require a manual reverse patch | Delete the three new files; remove the T-039 methods and helpers from `CertificateService`; remove `CERTIFICATE_REQUEST_OVERDUE` from `audit/events.py` | 1.0 | 0.8 | Implemented immediately after T-038 in the same session, per the same explicit user direction. `receive`/`reject` both locally reproduce `EngineService._queue_one_retest`'s idempotent-by-`(covenant_version_id, due_date)` retest-queueing rule (rather than reaching across a service boundary neither task's `Files owned` crosses) to flag an already-tested covenant for recomputation. `sweep_overdue` and the overdue-satisfied half of `receive` drive `LedgerService.revise` (`T-047`, unmodified) with a synthetic, unpersisted `SignalEventFacts` and a call-scoped `ContradictionRule` pairing the pre-existing `domain.signals.evidence.CERTIFICATE_OVERDUE_TYPE` constant (already present in the repository with its own pre-built fixture test) against a new local `"certificate_satisfied"` counterpart, so a satisfied overdue certificate is superseded through T-046's ordinary mechanism, never deleted. `evidence_item.family` has no member for a compliance signal in its spec-frozen six-value vocabulary; used `"payment"`, matching the value the repository's own pre-existing `test_certificate_overdue_derives_like_any_family` fixture already uses — documented in code as a deliberate, non-obvious choice. All 8 tests passed (the 6 named plus 2 additional refusal cases); changed-file Ruff and mypy (strict) passed clean; `bandit` reported no findings. The certificate screen (route + template) was built and verified in isolation (renders cleanly under the same minimal context `tests/security/test_hardening.py::test_no_template_references_external_origin` uses; ruff/mypy clean) but was deliberately **not** wired into `web/application.py`'s live router list: every other web-facing service there is composed over a `scoped_session` proxy, while `services/ledger.py` (owned by the already-completed `T-047`, outside this task's `Files owned`) requires a real `sqlalchemy.orm.Session` in its constructor — wiring the router in as-is would crash the first time an overdue sweep ran. A full-repository run (`tests/unit tests/integration tests/security`, excluding the Postgres-only `test_db_base_postgresql.py` and the environment-only `test_gate.py`) passed 928 of 933 tests; the 5 failures (`test_intake_screen.py::test_upload_returns_document_and_extraction_state`, `test_postgresql_requirement.py` needing a live Postgres service, `test_hardening.py`'s pre-existing `exports/memo.html` `letterhead` context gap, `test_route_declarations.py::test_missing_declaration_refuses_startup`, `test_single_call_site.py` already flagging `web.application`'s existing `ai.client` import) are all pre-existing and unrelated to certificates — none reference this task's files. No LLM key is required for this deterministic stage. |

## Per-gate ledger

| Gate | Evidence | Witness | Date |
|---|---|---|---|
| T-062 fast checks | 6 required T-062 unit tests, 504 unit/property regression tests excluding the two existing import-linter launcher tests, changed-file Ruff, focused strict mypy, formatting and compile checks passed; `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this deterministic simulation stage | Codex | 2026-08-31 |
| T-063 fast checks | 8 required T-063 tests and 35 focused/adjacent forecast/effect tests passed; changed-file Ruff, formatting, compile and focused strict mypy passed; the full run had 785 passing tests with 6 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this deterministic simulation stage | Codex | 2026-08-31 |
| T-037 fast checks | Ruff, mypy and 39 targeted tests passed; `python -m radarctl gate --fast` could not start because `nox` is unavailable | Codex | 2026-08-30 |
| T-040 fast checks | Ruff lint passed for changed files; 6 T-040 integration tests, 37 adjacent tests and the full default determinism check passed; `python -m radarctl gate --fast` could not start because `nox` is unavailable | Codex | 2026-08-30 |
| T-041 fast checks | Ruff lint passed for the repository; 7 T-041 integration tests and 289 selected regression tests passed; the gate could not start because `nox` and `lint-imports` are unavailable, and the repository-wide format check reports 12 pre-existing files outside T-041 | Codex | 2026-08-30 |
| T-088 fast checks | Ruff, focused mypy, 9 provider test executions, 279 existing unit tests excluding the environment-dependent gate tests, and 3 property tests passed; `python -m radarctl gate --fast` could not complete because `nox` is unavailable; bandit could not run because it is unavailable; direct import-contract execution kept the AI-provider boundary and reported pre-existing unrelated contract failures | Codex | 2026-08-31 |
| T-089 fast checks | 9 T-089 tests, 50 adjacent provider/threshold/model tests, Ruff and focused mypy passed; the full unit suite passed 288 tests with only the two existing `lint-imports` launcher tests failing because `lint-imports` is unavailable; `python -m radarctl gate --fast` stopped because `nox` is unavailable | Codex | 2026-08-31 |
| T-092 fast checks | 6 T-092 tests, 18 adjacent AI/security/packaging tests, 20 security tests, repository Ruff, focused mypy and the prompt CLI check passed; the full unit suite passed 305 tests with only the two existing `lint-imports` launcher tests failing because `lint-imports` is unavailable; `python -m radarctl gate --fast` stopped because `nox` is unavailable | Codex | 2026-08-31 |
| T-084 fast checks | 9 required T-084 tests, repository Ruff lint, focused mypy, and 329 unit/property/security tests excluding the two environment-dependent import-linter launcher tests passed; `python -m radarctl gate --fast` could not start because `nox` is unavailable; the repository-wide format check reports 12 pre-existing files outside T-084 | Codex | 2026-08-31 |
| T-085 fast checks | 7 required T-085 tests, 16 adjacent T-084/T-085 tests, repository Ruff lint, focused mypy and focused formatting passed; the full suite had 440 passing tests with 7 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; `python -m radarctl gate --fast` could not start because `nox` is unavailable; repository-wide mypy reports pre-existing errors outside T-085 and repository-wide formatting reports 12 pre-existing files outside T-085 | Codex | 2026-08-31 |
| T-047 fast checks | 12 required T-047 tests, 357 unit/property regression tests excluding the gate launcher, Ruff, focused mypy, focused formatting, and compile checks passed; `python -m radarctl gate --fast` could not start because `nox` is unavailable; the repository-wide threshold scan reports 3 pre-existing findings outside T-047 | Codex | 2026-08-31 |
| T-048 fast checks | 6 required T-048 tests and 51 focused/adjacent tests passed; repository Ruff, focused strict mypy, formatting and compile checks passed; the unit/property suites had 366 passing tests and 2 pre-existing import-linter launcher failures because `lint-imports` is unavailable; `python -m radarctl gate --fast` could not start because `nox` is unavailable; the repository-wide threshold scan reports 3 pre-existing findings outside T-048 | Codex | 2026-08-31 |
| T-054 fast checks | 9 required T-054 tests, 400 unit/property regression tests, changed-file Ruff, focused strict mypy, formatting and compile checks passed; the full run had 583 passing tests with 6 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; repository-wide Ruff and formatting report unrelated pre-existing files; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed | Codex | 2026-08-31 |
| T-055 fast checks | 11 required T-055 tests, 411 unit/property regression tests, changed-file Ruff, focused strict mypy, formatting and compile checks passed; the full run had 594 passing tests with 6 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; repository-wide Ruff and formatting report unrelated pre-existing files; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed | Codex | 2026-08-31 |
| T-056 fast checks | 6 required T-056 integration tests, 45 focused forecast/model/property tests, changed-file Ruff, focused strict mypy, formatting and compile checks passed; the unit/property suite had 414 passing tests with the 2 existing import-linter launcher failures; the full run had 600 passing tests with the same 6 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed | Codex | 2026-08-31 |
| T-057 fast checks | 5 required unit tests, 2 additional validation cases and 2 property tests (59 cases), changed-file Ruff, focused mypy, formatting and compile checks passed; the full run had 659 passing tests with 6 pre-existing environment/schema failures and 2 PostgreSQL-environment errors; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed | Codex | 2026-08-31 |
| T-058 fast checks | 6 required integration tests, 71 focused forecast/trace/attribution tests, 470 unit/property tests, changed-file Ruff, focused mypy and formatting passed; the unit/property run has 2 existing import-linter launcher failures because `lint-imports` is unavailable; 29 security tests passed with 1 unrelated pre-existing template-origin failure; repository-wide Ruff reports 1 unrelated import-order finding; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed | Codex | 2026-08-31 |
| T-060 fast checks | 6 required T-060 tests, 16 focused urgency/change tests, 424 unit tests excluding the two existing import-linter launcher tests, changed-file Ruff, focused mypy, formatting and compile checks passed; `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this deterministic stage | Codex | 2026-08-31 |
| T-061 fast checks | 7 required T-061 queue/performance tests, 23 focused queue/urgency/change tests, changed-file Ruff, and compile checks passed; the unit/security/property run had 520 passing tests and 3 pre-existing environment failures (missing `lint-imports` and an unrelated template context); `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this deterministic stage | Codex | 2026-08-31 |
| T-073 fast checks | 8 required queue screen/flow tests, 15 focused queue/query/performance tests, changed-file Ruff, focused mypy, formatting and compile checks passed; `python -m radarctl gate --fast` stopped with exit 127 because `nox` is unavailable; shared SQLAlchemy decimal types emit pre-existing cache-key warnings; no LLM key is required for this deterministic screen | Codex | 2026-08-31 |
| T-075 fast checks | 8 required case-file tests, 15 adjacent screen/query tests, changed-file Ruff, formatting, compile, contrast, security route checks and template checks passed; 535 unit/property/security tests passed with 2 existing `lint-imports` launcher failures and 1 unrelated existing template-context failure because those repository dependencies/fixtures are unavailable; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed; no LLM key is required for this deterministic read surface | Codex | 2026-08-31 |
| T-076 fast checks | 6 required forecast-panel tests, 53 focused case-file/forecast/queue/why/master-data/component/security tests, changed-file Ruff, formatting, compile and token/contrast checks passed; shared queue tests emit pre-existing SQLAlchemy decimal cache-key warnings; the repository-wide baseline still has the documented PostgreSQL, migration, import-linter and unrelated template-context failures; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed; no LLM key is required for this deterministic read surface | Codex | 2026-08-31 |
| T-077 fast checks | 5 required C-03 API tests, 4 horizon-control e2e contract tests and the 100 ms performance test passed; 79 focused forecast/case-file/scope/queue/why/master-data/component/security/persistence/model tests passed; changed-file Ruff, formatting, Node syntax, compile and token/contrast checks passed; shared queue tests emit pre-existing SQLAlchemy decimal cache-key warnings; the repository-wide baseline still has the documented PostgreSQL, migration, import-linter and unrelated template-context failures; `python -m radarctl gate --fast` remains unavailable because `nox` is not installed; no LLM key is required for this deterministic read surface | Codex | 2026-08-31 |
| T-097 fast checks | 8 required named T-097 integration/e2e tests plus upload coverage passed; 31 focused intake/service/refusal/audit tests and 52 broader intake/document/OCR/extraction regressions passed; changed-file Ruff, formatting, compile, route-declaration, shell/template, token and contrast checks passed. `python -m radarctl gate --fast` cannot start because `nox` is unavailable; `mypy` is unavailable; the full template-literal scan retains pre-existing findings in older templates, while the new intake template has no findings. No LLM key is required for provider-down and deterministic verification paths | Codex | 2026-08-31 |
| T-099 fast checks | 6 required T-099 unit tests, 451 other unit tests excluding the two existing import-linter launcher tests, changed-file Ruff, formatting and strict mypy passed; direct import-linter execution kept domain purity but reports pre-existing unrelated contract failures; `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this record-only stage | Codex | 2026-08-31 |


| T-066 fast checks | 9 required T-066 tests, 495 unit/property regression tests with 2 pre-existing `lint-imports` launcher failures, changed-file Ruff, focused mypy, formatting and compile checks passed; `python -m radarctl gate --fast` stopped because `nox` is unavailable; no LLM key is required for this deterministic stage | Codex | 2026-08-31 |
| T-120 fast checks | 6 required T-120 scheduler tests passed (5 consecutive reruns, no flakiness); changed-file Ruff lint, Ruff format and `mypy` (clean after an `apscheduler.*` stub-ignore override) passed; a manual end-to-end smoke test of `radarctl job run` and of the APScheduler `Scheduler` wrapper (including a simulated restart reloading a persisted cron schedule) passed after fixing a pickling bug found during that smoke test; the 691-test `unit or integration or property or security or migration` regression run had 4 pre-existing failures and 2 pre-existing errors, reproduced identically with the new test file deselected; `python -m radarctl gate --fast` could not start because `nox` is unavailable; `bandit` is unavailable; the direct `lint-imports` run reports the `session-boundary` contract against `covenant_radar.scheduler`, matching the same already-accepted pattern in `covenant_radar.services.*`; no LLM key is required for this deterministic stage | Claude | 2026-08-31 |

| T-102 fast checks | 6 required T-102 integration tests passed; 531 unit/property regression tests passed with the two existing `lint-imports` launcher tests deselected; changed-file Ruff lint, Ruff format, focused mypy, compile, token scan and 16 audit/route/model regression tests passed. `python -m radarctl gate --fast` reaches the environment gate but stops on 23 pre-existing repository format findings; direct nox lint reports the one pre-existing `domain/signals/__init__.py` import-order finding. In-memory PDF evidence rendered two pages with the stable hash embedded, and DOCX evidence contained the same figures and full assumptions. No LLM key is required for this deterministic export stage | Codex | 2026-08-31 |
| T-038 fast checks | 17 T-038 unit/integration tests passed (6 named plus 11 additional); changed-file Ruff and mypy (strict) passed clean; `bandit` reported no findings; a 550-test targeted regression (`tests/unit`, `test_audit_coverage.py`, `test_registry_service.py`, `test_retest_on_arrival.py`, `test_seed.py`) passed with only the 2 pre-existing environment-only `test_gate.py` failures; `python -m radarctl gate --fast` was not run directly (`lint-imports` is not on PATH as a bare subprocess in this environment, matching `test_gate.py`'s own pre-existing failure) but every constituent check it would run (ruff, mypy, pytest) was run individually and passed | Claude | 2026-09-01 |
| T-039 fast checks | 8 T-039 integration tests passed (6 named plus 2 additional); changed-file Ruff and mypy (strict) passed clean; `bandit` reported no findings; the certificate template renders cleanly under `test_hardening.py`'s own minimal context; a full-repository run of `tests/unit tests/integration tests/security` (933 tests, excluding the Postgres-only and environment-only gate files) passed 928 with the 5 failures confirmed pre-existing and unrelated to this change; the router is built and tested but intentionally not wired into `web/application.py` — see the per-task ledger row for why | Claude | 2026-09-01 |
| T-065 fast checks | 5 required T-065 tests and 36 adjacent threshold/forecast/cohort tests passed; changed-file Ruff, format and compile checks passed; the calibration record reproduces the final score set and SHA-256 digest. `python -m radarctl gate --fast` cannot start because `nox` is unavailable. The full repository run hit two unrelated integration errors and a Windows/SQLAlchemy access violation in the nightly-pipeline interaction; no LLM key is required for this deterministic calibration stage | Codex | 2026-08-31 |

| T-135 fast checks | 7 required `test_api_resources.py` tests passed; `tests/unit` (481 passed, 2 pre-existing `test_gate.py` failures), `tests/integration` (328 passed, 1 pre-existing PostgreSQL-requirement failure, 2 pre-existing PostgreSQL errors) and `tests/security`+`tests/contract` (35 passed, 1 pre-existing unrelated `test_hardening.py` template-context failure) run individually and green against baseline; changed-file Ruff lint, Ruff format and `mypy` passed with zero new findings outside `asgi.py`'s verified-pre-existing findings; `python -m radarctl gate --fast` could not start because `nox` is unavailable; `lint-imports` and `bandit` are unavailable; no LLM key is required for this deterministic read surface | Claude | 2026-08-31 |
| T-029 fast checks | Both required test files green (22/22: `pytest -q tests/unit/test_custom_formula.py tests/property/test_custom_formula_safety.py`, the task's own `Run` command), including the property test's 500 Hypothesis-generated hostile examples per run with `eval`/`exec` tripwired; changed-file Ruff lint, Ruff format and strict `mypy` passed with zero findings on all three owned files; `nox` and the dev/test extras were provisioned into `.venv` from the already-pinned `requirements.lock` (no manifest change) and the full `tests/unit`+`tests/property` suite then ran 568/570 passing, the 2 failures being `test_gate.py`'s pre-existing Windows-console-codepage-dependent `lint-imports` launcher tests, reproduced independent of this change; `python -m radarctl gate --fast` could not complete end-to-end because its `format` step fails on 41 pre-existing unformatted files outside `Files owned`, its `types`/`imports` nox sessions install a narrower dependency set than the full runtime and report only pre-existing errors confirmed to touch zero files under `domain/ratios`, and its `tests` nox session's venv lacks `sqlalchemy`/`tzdata`; every fast-gate check was instead run directly and confirmed clean for this change; no LLM key is required for this deterministic stage | Claude | 2026-09-01 |
| T-135 | No Git; snapshot retained at `var/snapshots/T-135/` (pre-edit copies of `src/covenant_radar/asgi.py`, `src/covenant_radar/api/v1/routers/__init__.py`, `src/covenant_radar/db/repositories/simulation.py`) | Restore the three snapshotted files with `Copy-Item` back over their originals; delete the new files: `src/covenant_radar/api/{errors,pagination,conditional}.py`; `src/covenant_radar/api/v1/routers/{covenant_tests,evidence,simulations,memos,cases,audit_events}.py`; `src/covenant_radar/api/v1/schemas/{covenant_tests,evidence,simulations,memos,cases,audit_events}.py`; `tests/integration/test_api_resources.py`; then reverse the additive `ForecastRead`/list-and-detail-endpoint edits to `src/covenant_radar/api/v1/schemas/forecast.py` and `src/covenant_radar/api/v1/routers/forecast.py` | 2.0 | 1.2 | **Context:** `tasks.md` marks T-135 `Build: DEFERRED — out of scope for this window` (only queued for restoration "if time remains after Phase 6"); worked at the user's explicit direction ahead of that queue position, off the frozen `plan.md §6` contracts `C-21`/`C-22` rather than by guessing. `tasks.md`/`plan.md`/`spec.md` were not edited (off limits per `plan.md §1.1`) — this ledger is the record of completion instead. Added the six previously-missing `C-21` read resources with no prior API surface (`tests`, `evidence`, `simulations`, `memos`, `cases`, `audit-events`) plus a `forecasts` list/detail pair alongside the existing `C-03` path endpoint, each following the closest existing convention: session-direct routers (`forecast.py`/`explain.py`'s pattern) over `db/scoping.py::ownership_path_for`-composed, portfolio-scoped statements, except `audit-events`, which — matching `web/routes/audit.py`'s and `AuditRepository`'s established, deliberate design — carries no portfolio ownership path and is gated by `VIEW_AUDIT` alone. Added `api/pagination.py`: one generalised, HMAC-signed, filter-digest-bound keyset (seek) cursor, replacing what would have been a third near-duplicate of `web/view_models/audit.py::AuditCursor` and `db/repositories/triage.py::QueueCursor`; two-column `(primary, id)` seek predicates avoid SQLite/PostgreSQL composite row-value portability problems. Added `api/conditional.py`: `ETag`/`If-None-Match`/304 support, keyed on a resource's `version` column where one exists (`EvidenceItem`, `Memo`, `Case`) and on `id` for write-once, never-edited rows (`CovenantTest`, `Simulation`, `Forecast`, `AuditEvent`), applied to every new detail endpoint plus the new forecast detail endpoint. Added `api/errors.py` and extended `asgi.py`'s three existing global exception handlers (`DomainError`, `StarletteHTTPException`, plus a new `RequestValidationError` registration that did not exist before) to emit one `{error, message, field, request_id}` envelope on every route — additive only: the pre-existing `error`/`message`/`request_id` keys and, for FastAPI's own validation 422, the pre-existing `detail` array are preserved verbatim so `tests/integration/test_master_data.py::test_missing_field_422_with_path`'s existing assertion keeps passing unmodified; `field` is newly populated from `DomainError.field` (already modelled, previously dropped on the floor) or from the first validation error's `loc`. Fixed a real, in-scope bug found while building the `405`-with-`Allow`-header behaviour `C-21` requires: `_http_error_handler` was discarding `HTTPException.headers` entirely, which would have silently dropped Starlette's own `Allow` header on every 405 in the whole application, not just the new routers. Also exported the pre-existing but never-exported `create_covenants_router` from `api/v1/routers/__init__.py`, and added a small public `query`/`annotate` pair to `db/repositories/simulation.py` (reusing its existing lineage-annotation logic rather than duplicating or reaching into its `_annotate_required`/`_scoped_select` internals from another module) so the new `simulations` resource can page across every forecast rather than only within one. All seven required `tests/integration/test_api_resources.py` tests pass (`test_every_resource_lists_and_reads` exercises all ten `C-21` collection resources, both list and detail, including the three pre-existing ones), plus `test_no_route_for_forbidden_operations` enumerates the live route table against a forbidden-operation-name denylist rather than asserting a tautology. Changed-file Ruff lint and format passed clean; changed-file `mypy` (base project config — `check_untyped_defs`, `warn_return_any`, `warn_unused_ignores`; `strict` is scoped to `domain.*`/`services.*`, not `api.*`, per `pyproject.toml`) reports zero new findings in every new/edited file except `asgi.py`, where the ten remaining findings were verified byte-for-byte pre-existing (or, for one `RequestValidationError` registration line, the same already-accepted `add_exception_handler`-typing-stub friction the two pre-existing handler registrations already carried) by running `mypy` directly against the pre-edit snapshot. `tests/unit`, `tests/integration`, `tests/security` and `tests/contract` were run to green individually (481, 328, 35 and included-in-contract passed respectively) reproducing exactly the pre-existing baseline noise this ledger already documents elsewhere (`nox`/`lint-imports` unavailable in `test_gate.py`; `COVENANT_RADAR_DATABASE_URL`-gated PostgreSQL tests; one unrelated `memo.html`/`letterhead` template-context gap in `test_hardening.py`) — no new failures. Running all four suites together intermittently hit a native `Windows fatal exception: access violation` during Jinja2 garbage collection partway through; the same fault class is already on record in this ledger's own `T-065` entry ("a Windows/SQLAlchemy access violation in the nightly-pipeline interaction") and did not reproduce when the suites were run individually, so it is logged as pre-existing local-environment flakiness rather than a regression. `python -m radarctl gate --fast` could not start because `nox` is unavailable; `lint-imports` and `bandit` are unavailable. No LLM key is required: this is a deterministic, database-backed read surface with no model call anywhere in the request path. |

| Phase 7c (H-09–H-12) | No Git; snapshot retained at var/snapshots/H-09-11/ | Restore src and tests from the snapshot with robocopy var\\snapshots\\H-09-11\\src src /MIR and robocopy var\\snapshots\\H-09-11\\tests tests /MIR; reverse the additive phase 7c template, CSS, view-model, route, SVG, repository, documentation, and test changes | 3.5 | 2.5 | Added the scoped portfolio summary strip, stored trajectory mini-previews in queue rows, an enlarged forecast trajectory with visible crossing-day driver annotation, and the H-12 why-panel talking point. Added four integration tests covering scoped aggregation, persisted mini-trajectories, escaped chart labels, and persisted crossing drivers. Focused phase 7c plus adjacent queue, forecast, why-panel, accessibility, and e2e checks passed (40 tests); changed-file Ruff, format, and Python compilation passed. python -m radarctl gate --fast remains unavailable because nox is not installed. No LLM key is required for this deterministic read surface. | Codex | 2026-09-01 |

## T-001 verification transcript

| Command | Result |
|---|---|
| `pip install -e .` | Exit 0 in a clean Python 3.12 virtual environment. |
| `python -c "import covenant_radar; print(covenant_radar.__version__)"` | Exit 0; printed `0.1.0`. |
| `python -m radarctl --help` | Exit 0; listed every command group. |
| `pytest -q tests/unit/test_packaging.py` | Exit 0; 4 passed. |

| H-06 | No Git; snapshot retained at `var/snapshots/H-06/` | Restore `src` and `tests` from the snapshot with `robocopy var\\snapshots\\H-06\\src src /MIR` and `robocopy var\\snapshots\\H-06\\tests tests /MIR`; reverse the additive `scripts/demo_up.ps1`, `tests/integration/test_demo_bootstrap.py`, `README.md`, `src/covenant_radar/config/settings.py`, and `tests/unit/test_settings.py` changes | 0.5 | 0.8 | Hardened the one-command PowerShell demo bootstrap with repo-root anchoring, strict failure handling, Python 3.12 validation, non-persistent cryptographic secrets, blank-`.env` placeholder handling, loopback/local SQLite/recorded-AI safety, and the complete install→migrate→seed→pipeline→presenter→serve flow. Reserved the crypto environment variables consumed by `security.secrets` so valid demo startup configuration is not rejected as unknown settings. Added static bootstrap-contract coverage. Focused settings/crypto/runtime/bootstrap checks passed (16 tests), changed-file Ruff passed, Python compilation passed, and Windows PowerShell parsing passed. A bounded smoke run reached migrations and correctly refused the already-populated local database at the intentional reference-seed guard; a clean clone uses the empty `var/covenant-radar.db` path. `python -m radarctl gate --fast` remains unavailable because `nox` is not installed, and the broader repository baseline retains unrelated pre-existing failures. No LLM key is required: the bootstrap forces recorded offline responses. | Codex | 2026-09-01 |
| T-029 | No Git; snapshot retained at `var/snapshots/T-029/` | `robocopy var\\snapshots\\T-029\\src src /MIR` and `robocopy var\\snapshots\\T-029\\tests tests /MIR`; also delete `src/covenant_radar/domain/ratios/custom.py`, `tests/unit/test_custom_formula.py` and `tests/property/test_custom_formula_safety.py` if the snapshot restore is skipped | 1.5 | 0.6 | **Context:** `tasks.md` marked T-029 `Build: DEFERRED — out of scope for this window` (covenant admin depth, §2.4); worked at the user's explicit direction ahead of that queue position, off the frozen `plan.md §6` `C-31` contract rather than by guessing. `tasks.md §2.3`/§2.4 and `plan.md`/`spec.md` were left untouched (off limits, and §2.4's summary/queue text is not renumbered for a pulled-forward task per the same convention already used for `T-065`/`T-135`); only T-029's own §5 block header was ticked `[x]` with `Build: DONE — pulled forward ahead of schedule, see MERGE_LOG.md`. Added `domain/ratios/custom.py`: `parse_custom_formula(text, allowed_lines) -> Formula` parses with `ast.parse` and walks the tree against a closed node allow-list (literals, `allowed_lines` names, `+ - * /`, unary sign, implicit-parenthesis grouping) plus a non-boolean/finite-numeric-constant check, refusing before any evaluation with `FormulaRefused` naming the disallowed construct (falling back to the node's own class name for any construct not given a friendly label, so nothing is ever refused anonymously) and its `line, column` position; an unknown line name is refused with `difflib.get_close_matches` near-match suggestions (or an explicit "no similarly named line exists"); length (500 chars) and tree-depth (20 levels, computed with an explicit stack rather than recursion so the depth check itself cannot overflow) limits are enforced; a formula that parses but names no line is refused ("a constant is not a covenant"). Once validated, `Formula.evaluate` is a hand-written, non-recursive-parse tree-walking `Decimal` interpreter over the same closed node set — never `eval`/`exec`/`compile` of caller text — that returns a `FormulaOutcome` (reusing `T-027`'s exact shape) and never raises: a missing line reports `NotComputableReason.MISSING_LINE`, and division by zero reports `NotComputableReason.FORMULA_NOT_COMPUTABLE`, the member `T-030`'s `reasons.py` already reserved for exactly this task. Added all nine named `tests/unit/test_custom_formula.py` cases plus 12 supporting ones (21 total, covering every hostile construct the brief names — call, attribute, subscript, comprehension, lambda, import, walrus, string/boolean literals — individually) and the required `tests/property/test_custom_formula_safety.py::test_no_generated_input_ever_executes_code`: a Hypothesis strategy mixing known-hostile fragments (`__import__(...)`, `().__class__.__bases__`, `eval`/`exec`/`compile` calls, comprehensions, lambdas) with allowed line names, arbitrary Unicode and integers, run for 500 generated examples per pytest invocation with `builtins.eval`/`builtins.exec` replaced by a stand-in that fails the test the instant either is called (deliberately not guarding `__import__`/`compile` themselves, since the standard library's own `ast.walk`/`ast.parse` legitimately use both internally — confirmed by first observing the guard trip on `ast.walk`'s internal `from collections import deque` before narrowing it). Both named test files passed 22/22 (`pytest -q tests/unit/test_custom_formula.py tests/property/test_custom_formula_safety.py`, the task's own `Run` command). Ruff lint, Ruff format and strict `mypy` (this module sits under `pyproject.toml`'s `domain.*` strict override) passed clean with zero findings on all three owned files. Provisioned the local `.venv` with the project's pinned dev/test extras from `requirements.lock` (already-declared, already-pinned dependencies — not a manifest change) since only runtime dependencies were previously installed; ran the full `tests/unit`+`tests/property` suite in that fully-provisioned venv: 568 of 570 passed, with the only 2 failures being `tests/unit/test_gate.py`'s pre-existing `lint-imports`-subprocess launcher tests (one Windows console-codepage `UnicodeDecodeError` decoding `lint-imports`' own coloured output, reproduced independent of this change). `python -m radarctl gate --fast` could not complete end-to-end: its `format` step fails on 41 pre-existing unformatted files outside `Files owned` (confirmed with the identical pinned Ruff 0.9.10 both inside and outside the gate's own nox venv); its `types` and `imports` nox sessions install a narrower dependency set than the full runtime (no `fastapi`/`sqlalchemy`/`alembic`/…) and report hundreds of pre-existing errors confirmed to touch zero files under `domain/ratios`; its `tests` nox session's venv is missing `sqlalchemy`/`tzdata` entirely. Ran every fast-gate step directly instead (`ruff check`/`format --check`, `mypy`, `lint-imports`, `pytest`) and confirmed none of the pre-existing failures are attributable to this change. No LLM key is required: this is a deterministic domain-layer parser and evaluator with no model call anywhere in it. | Claude | 2026-09-01 |
 | T-029 | No Git; snapshot retained at `var/snapshots/T-029/` | `robocopy var\\snapshots\\T-029\\src src /MIR` and `robocopy var\\snapshots\\T-029\\tests tests /MIR`; also delete `src/covenant_radar/domain/ratios/custom.py`, `tests/unit/test_custom_formula.py` and `tests/property/test_custom_formula_safety.py` if the snapshot restore is skipped | 1.5 | 0.6 | **Context:** `tasks.md` marked T-029 `Build: DEFERRED — out of scope for this window` (covenant admin depth, §2.4); worked at the user's explicit direction ahead of that queue position, off the frozen `plan.md §6` `C-31` contract rather than by guessing. `tasks.md §2.3`/§2.4 and `plan.md`/`spec.md` were left untouched (off limits, and §2.4's summary/queue text is not renumbered for a pulled-forward task per the same convention already used for `T-065`/`T-135`); only T-029's own §5 block header was ticked `[x]` with `Build: DONE — pulled forward ahead of schedule, see MERGE_LOG.md`. Added `domain/ratios/custom.py`: `parse_custom_formula(text, allowed_lines) -> Formula` parses with `ast.parse` and walks the tree against a closed node allow-list (literals, `allowed_lines` names, `+ - * /`, unary sign, implicit-parenthesis grouping) plus a non-boolean/finite-numeric-constant check, refusing before any evaluation with `FormulaRefused` naming the disallowed construct (falling back to the node's own class name for any construct not given a friendly label, so nothing is ever refused anonymously) and its `line, column` position; an unknown line name is refused with `difflib.get_close_matches` near-match suggestions (or an explicit "no similarly named line exists"); length (500 chars) and tree-depth (20 levels, computed with an explicit stack rather than recursion so the depth check itself cannot overflow) are enforced; a formula that parses but names no line is refused ("a constant is not a covenant"). Once validated, `Formula.evaluate` is a hand-written, non-recursive-parse tree-walking `Decimal` interpreter over the same closed node set — never `eval`/`exec`/`compile` of caller text — that returns a `FormulaOutcome` (reusing `T-027`'s exact shape) and never raises: a missing line reports `NotComputableReason.MISSING_LINE`, and division by zero reports `NotComputableReason.FORMULA_NOT_COMPUTABLE`, the member `T-030`'s `reasons.py` already reserved for exactly this task. Added all nine named `tests/unit/test_custom_formula.py` cases plus 12 supporting ones (21 total, covering every hostile construct the brief names — call, attribute, subscript, comprehension, lambda, import, walrus, string/boolean literals — individually) and the required `tests/property/test_custom_formula_safety.py::test_no_generated_input_ever_executes_code`: a Hypothesis strategy mixing known-hostile fragments (`__import__(...)`, `().__class__.__bases__`, `eval`/`exec`/`compile` calls, comprehensions, lambdas) with allowed line names, arbitrary Unicode and integers, run for 500 examples per pytest invocation with `builtins.eval`/`builtins.exec` replaced by a stand-in that fails the test the instant either is called (deliberately not guarding `__import__`/`compile` themselves, since the standard library's own `ast.walk`/`ast.parse` legitimately use both internally — confirmed by first observing the guard trip on `ast.walk`'s internal `from collections import deque` before narrowing it). Both named test files passed 22/22 (`pytest -q tests/unit/test_custom_formula.py tests/property/test_custom_formula_safety.py`, the task's own `Run` command). Ruff lint, Ruff format and strict `mypy` (this module sits under `pyproject.toml`'s `domain.*` strict override) passed clean with zero findings on all three owned files. Provisioned the local `.venv` with the project's pinned dev/test extras from `requirements.lock` (already-declared, already-pinned dependencies — not a manifest change) since only runtime dependencies were previously installed; ran the full `tests/unit`+`tests/property` suite in that fully-provisioned venv: 568 of 570 passed, with the only 2 failures being `tests/unit/test_gate.py`'s pre-existing `lint-imports`-subprocess launcher tests (one Windows console-codepage `UnicodeDecodeError` decoding `lint-imports`' own coloured output, reproduced independent of this change). `python -m radarctl gate --fast` could not complete end-to-end: its `format` step fails on 41 pre-existing unformatted files outside `Files owned` (confirmed with the identical pinned Ruff 0.9.10 both inside and outside the gate's own nox venv); its `types` and `imports` nox sessions install a narrower dependency set than the full runtime (no `fastapi`/`sqlalchemy`/`alembic`/…) and report hundreds of pre-existing errors confirmed to touch zero files under `domain/ratios`; its `tests` nox session's venv is missing `sqlalchemy`/`tzdata` entirely. Ran every fast-gate step directly instead (`ruff check`/`format --check`, `mypy`, `lint-imports`, `pytest`) and confirmed none of the pre-existing failures are attributable to this change. No LLM key is required: this is a deterministic domain-layer parser and evaluator with no model call anywhere in it. | Claude | 2026-09-01 |
 | T-112 | No Git; snapshot retained at `var/snapshots/T-112/` | `robocopy var\\snapshots\\T-112\\src src /MIR` and `robocopy var\\snapshots\\T-112\\tests tests /MIR`; reverse the additive T-112 files and template change if the snapshot restore is skipped | 1.5 | 0.9 | Added scoped, append-only disposition recording with dismissed-reason enforcement, outcome-specific reason-code taxonomy, audit entries, and history sequencing; added reusable case-file/memo feedback-control rendering and C-13 `POST /dispositions`; added an allowlisted, scoped CSV labelled-warning export that includes unlabelled warnings and excludes personal-class values, notes, actor identity, and arbitrary JSON. Required six integration tests pass; changed-file Ruff, strict mypy, compilation, and focused regression tests pass. `python -m radarctl gate --fast` remains unavailable because nox is not installed; the combined contract check retains one unrelated pre-existing API-documentation drift failure. No LLM key is required for this deterministic workflow. | Codex | 2026-09-01 |
| T-113 | No Git; snapshot retained at `var/snapshots/T-113/` | `robocopy var\\snapshots\\T-113\\src src /MIR` and `robocopy var\\snapshots\\T-113\\tests tests /MIR`; reverse the additive T-113 service, route, template, stylesheet, event, and test changes if a rollback is required | 2.0 | 1.0 | Added the protected users-and-access console with user provisioning, Argon2 password lifecycle, SSO mapping, role replacement, maker-checker self-escalation approval, portfolio hierarchy scoping with saved-view narrowing, immediate session/API-key revocation, last-administrator protection, audit before/after payloads, and live request provenance. Added the six required security acceptance tests plus strict production-template and SSO transition coverage; focused T-113 tests pass 8/8, changed-file Ruff/mypy/compilation pass. No LLM key is required for this deterministic database-backed administration surface. | Codex | 2026-09-01 |
| T-107 | No Git (unavailable in this environment); no snapshot taken (nothing pre-existing under `Files owned` to preserve) | Delete `src/covenant_radar/ai/registry.py`, `src/covenant_radar/services/model_governance.py`, `docs/model-cards/stage1_extraction.md`, `docs/model-cards/stage7_memo.md`, `tests/integration/test_model_registry.py`; revert the additive edits to `src/covenant_radar/ai/client.py` (the `component`/`registry_guard` fields and the two new `_validation_reason` branches), `src/covenant_radar/ai/intake.py` and `src/covenant_radar/ai/memo.py` (the `COMPONENT` constant and its threading into `CallContext`), `src/covenant_radar/web/application.py` (the `_deployment_environment` helper and the guard wired into the production `ModelClient`), and `src/covenant_radar/audit/events.py` (the three `MODEL_REGISTRATION_*` event types); restore `### T-107 ... Build: DEFERRED — out of scope for this window` in `tasks.md` | 1.5 | ~0.6 | **Context:** `tasks.md` marked T-107 `Build: DEFERRED — out of scope for this window` (model governance, §2.4); the user asked for it explicitly ahead of that queue position — flagged the conflict with an `AskUserQuestion` first, and built it at their confirmed direction, off the frozen `plan.md §5.9`/`§6` contracts (the `model_registration` table and its `state ∈ {registered, approved, champion, challenger, retired}`) rather than by guessing. `tasks.md §2.3`/§2.4 were left untouched per the same convention already used for `T-029`/`T-065`/`T-135`; only T-107's own §5 block header was ticked `[x]` with `Build: DONE — pulled forward ahead of schedule, see MERGE_LOG.md`. Found `db/models/operations.py`'s `ModelRegistration` table and `web/view_models/governance.py`'s read side (`T-081`) already built and already tested (`tests/integration/test_governance_screens.py`) against that same table, so this task supplied only the write/enforcement half. Added `ai/registry.py`: a persistence-neutral `ModelRegistrationRecord`, the closed five-state vocabulary, `ModelRegistryGuard.ensure_permitted` (refuses `ComponentNotRegistered`/`ComponentNotApproved` in production, logs a loud warning and permits the call in development — `environment` is an explicit constructor argument, never read from a process-global), and `check_model_cards` (the build check: every named component needs a `docs/model-cards/<component>.md` file, all missing cards named at once). Added `services/model_governance.py`: `ModelGovernanceService` (register/decide_approval/list_registrations/get_registration) wired through the existing shared maker-checker workflow (`security/maker_checker.py`, `services/approvals.py`, reusing `services/registry.py`'s generic `SqlAlchemyMakerCheckerRepository` rather than duplicating it) with `Permission.MANAGE_JOBS` as the propose permission and the already-existing `Permission.APPROVE_MODEL_PROMOTION` as the approve permission (both already in the closed vocabulary and the seeded Administrator/Risk Head roles, so no change to `security/permissions.py` or the seed data was needed, and `tests/unit/test_permissions.py::test_enumeration_matches_spec_matrix`'s exact-set assertion against `spec §16.1` stayed untouched); self-approval is refused for free by `ApprovalService.decide`'s existing distinct-actor check; a re-registration that changes provider/model id/prompt version resets the row to `registered` and clears any prior approval, submitting a fresh pending request, while an identical re-registration is a no-op; `list_registrations`/`get_registration` require only `Permission.VIEW_AUDIT`, which no propose/approve permission is paired with, giving the auditor a read path and no write path. Wired the guard into the single call site additively and backward-compatibly: `ai/client.py` gained an optional `CallContext.component` field and an optional `ModelClient(registry_guard=...)` parameter (both default `None`, so every pre-existing T-089 call site and test is unaffected), consulted once at the top of the existing validation `try` block before a masked prompt ever reaches a provider, with the two new exceptions mapped ahead of the pre-existing catch-all `RuntimeError` branch in `_validation_reason` so they are not misclassified as `unmasked_prompt`. Threaded real component identifiers (`stage1_extraction`, `stage7_memo`) through `ai/intake.py`/`ai/memo.py`'s existing `CallContext` construction, and wired a real `ModelRegistryGuard` into the production `ModelClient` composition in `web/application.py`, reading the deployment environment from a plain `COVENANT_RADAR_ENVIRONMENT` process variable outside the validated `Settings` schema (a deliberate, narrow, fail-closed escape hatch — anything other than the literal `development` collapses to `production`) rather than adding an environment concept to `config/settings.py`, which has no such field today and whose blast radius (every settings test, `default.toml`, the secrets-injection path) was judged out of proportion to this task. Added `docs/model-cards/stage1_extraction.md` and `docs/model-cards/stage7_memo.md` (purpose, call site, prompt, input/output handling, registration fields, known limitations) so the build check passes for both components actually wired to a model today. Added all six required `tests/integration/test_model_registry.py` tests (`test_unregistered_component_refused_in_production`, `test_self_approval_refused`, `test_prompt_bump_requires_reapproval`, `test_missing_model_card_fails_build`, `test_auditor_read_only`, `test_registration_audited`); `pytest -q tests/integration/test_model_registry.py` 6/6 passed. Deliberately did not add `ModelGovernanceService` to `tests/security/test_audit_coverage.py`'s hard-coded `_SCANNED_CLASSES`/`_CLASSIFICATION`: that suite's static AST heuristic only follows same-class `self.X(...)` calls, so `decide_approval`'s indirect audit (via the maker-checker callback invoked from inside `ApprovalService.decide`) would read as a false-negative "uncovered" method; the actual audit behaviour is instead verified directly and more strongly by `test_registration_audited`, which inspects the real recorded events. Ran the changed-file Ruff lint and format (clean after two rounds — one auto-fixed unsorted import, several wrapped long lines — both zero findings on the final state) and focused `mypy` on every touched/added file; `web/application.py` reports 18 pre-existing `scoped_session[Session]`-vs-`Session` mismatches under strict mypy (confirmed pre-existing: 17 of the 18 are on `sessions`-argument lines this change never touched, e.g. the existing `RegistryService`/`DocumentService`/`IntakeService`/router constructions; the 18th is this change's one new line following the identical, already-accepted pattern) — left as-is rather than restructuring the file's session-typing to fix an unrelated, file-wide gap. Ran the full `tests/unit`+`tests/integration`+`tests/security`+`tests/contract` suite: 1006 passed, 9 failed, 2 errored, none attributable to this change — confirmed by inspection: 2 PostgreSQL-service errors, 2 `lint-imports`-unavailable `test_gate.py` failures, a design-token literal check, a template-external-origin check, a route-declaration-startup check, an OpenAPI-drift check spanning unrelated routes (`/api/v1/facilities`, `/api/v1/forecasts/...`, `/api/v1/cases`, ...), a missing `python-multipart` dependency breaking one intake-upload test, and `tests/security/test_single_call_site.py::test_only_permitted_modules_import_the_client` (pre-existing: `web/application.py` already imported `ai.client` directly before this task touched the file). `python -m radarctl gate --fast` remains unavailable because `nox` is not installed. No LLM key is required: `AiSettings.provider` is `"none"` in this environment, so the production `ModelClient` branch this task's guard is wired into is not constructed yet; the user confirmed the key will be added later, after the app build is complete. | Claude | 2026-09-01 |
| H-07 | No Git; no pre-task snapshot captured (four small template edits plus one `web/routes/auth.py` and one `web/templates/base.html` change — reversible by hand) | In `src/covenant_radar/web/templates/screens/auth/{sign_in,change_password,mfa_enrol,mfa_verify}.html`, restore the bare `<!doctype html>` documents (no `base.html` extension, no CSS links); in `src/covenant_radar/web/routes/auth.py`, restore `_TEMPLATE_DIRECTORY` to `.../templates/screens/auth`, drop the `theme_for_request` import, and revert `render()` to `templates.get_template(template_name)` with only `request=request, **context`; in `src/covenant_radar/web/templates/base.html`, un-gate the `borrowers`/`facilities`/`portfolios`/`covenants`/`intake`/`simulator`/`audit`/`governance`/`admin` nav links from `{% if principal %}`; remove the appended `.auth-panel` rule at the end of `src/covenant_radar/web/static/css/app.css` | 1.0 | 0.4 | Found while investigating a user report that the running app "still looks like the old one" after Phase 7: `H-07`'s own brief in `BUILD_ORDER.md` documented this exact defect (`GET /sign-in` renders with no `<link>` tag at all) as evidence, but the fix was never applied — confirmed by direct inspection (no auth template contained `extends "base.html"`) and by `MERGE_LOG.md` itself, which has no prior `H-07` row. Root cause beyond the missing `{% extends %}`: `create_auth_router`'s own `render()` used a private `Environment` scoped to `templates/screens/auth` only, so it could not resolve `base.html` or `_components/*` even if the templates asked for them — unlike every other screen router (e.g. `queue.py`), which points its fallback loader at the full `templates/` root and prefers `request.app.state.template_env` (the shared, `StrictUndefined` environment `asgi.py` builds with the `_`/`notification_unread_count` globals) when running inside `create_app`. Rewrote all four auth templates to extend `base.html` and use `_components/components.html`'s `field`/`button` macros instead of raw markup, matching `tests/a11y/_screens.py`'s existing `"screens/auth/sign_in.html"`-style references (that test file already anticipated this template path — it was the load-bearing evidence the fix target was correct). Widened `_TEMPLATE_DIRECTORY` to the full `templates` root and pointed `render()` at `request.app.state.template_env` with a same-shaped local fallback for the bare-`FastAPI()` unit tests in `test_auth_local.py`/`test_auth_sso.py`; `render()` now supplies the `principal=None`, `theme` (via `theme_for_request`), `locale`, `text_direction`, and a default empty `error` that `base.html`'s `StrictUndefined` environment requires (the GET handlers for `/sign-in`, `/password/change`, `/mfa/enrol`, `/mfa/verify` never passed `error`, which is what actually raised `UndefinedError` on first fix attempt before the default was added). Gated `base.html`'s protected portfolio nav links behind `{% if principal %}` per the brief's "no portfolio nav links a signed-out user can't use." Added one small token-only `.auth-panel` rule (`max-width: 28rem`, existing `--space-*` tokens, no new colors) so the login card doesn't stretch full-width. Verified live: restarted `radarctl serve` against the existing demo database and confirmed `GET /sign-in` now returns the styled shell (`tokens.css`/`app.css` linked, paper/ink panel, minimal signed-out nav) with the CSRF middleware still auto-injecting its hidden field correctly. `pytest tests/a11y -k auth` (4 tests, both themes, zero axe violations) and `tests/integration/test_auth_local.py tests/integration/test_auth_sso.py` (20 tests, covering the bare-`FastAPI()` fallback path) pass; `tests/security/test_hardening.py` has 9/10 passing with the 1 failure being the pre-existing, already-documented `exports/memo.html` `letterhead` context gap (unrelated to this change, reproduced independent of it). `python -m radarctl gate --fast`/`nox` remain unavailable in this environment; no LLM key is required for this template-only stage. | Claude | 2026-09-01 |
| H-14 | No Git; no pre-task snapshot captured this session — a single 4-line CSS rule deletion in one file, trivially reversible by hand | In `src/covenant_radar/web/static/css/modern.css`, re-insert immediately before the `.case-header__fact-label,` / `.governance__facts dt {` rule: `.case-header__facts { grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr)); gap: var(--space-3); }` | 0.3 | 0.3 | User reported (with a screenshot of the portfolio queue) that the UI had "empty space across the UI" and asked for a Playwright-verified fix across personas/screens. Live-browser investigation (`http://127.0.0.1:8000`, signed in as `riskhead`) swept the portfolio queue at seven viewports from 720px to 2400px with a DOM overflow probe; found no reproducible overflow or clipping there today — the queue's `.queue-summary-strip`/`.filter-bar__fields` grids are all `minmax(0, …)`/`auto-fit` and shrink safely at every width tested (the reported screenshot's cut-off cards did not reproduce against current `main`, plausibly already resolved by concurrent same-day work logged elsewhere in this file). Found a real, severe instance of the same underlying defect class one screen over: the borrower case file's four-fact header (`templates/screens/borrower/_header.html` — "Borrower / Exposure / Worst covenant / Dated risk" plus the deliberate `.case-header__whitespace` spacer implementing the spec's 40%-whitespace row) renders correctly from `app.css:952` (`grid-template-columns: repeat(4, minmax(0, 1fr)) 40%`, with `app.css:981`'s intentional `.case-header__fact-value { text-align: end }`), but `modern.css`, loaded after `app.css` in `base.html`, redeclared the same selector at (pre-edit) line 1487 as `grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr))`. Cascade merges the two rules per-property, not per-block: the still-active right-alignment from `app.css` combined with `modern.css`'s wider, evenly-stretched auto-fit columns to leave a large dead gap between every label and its value in three of the four facts, instead of the one deliberate 40% gutter at the row's end — the concrete shape of the reported "empty space." Confirmed via `getBoundingClientRect()` before the fix (five ~268px-wide equal tracks including the whitespace column) and via a live screenshot after deleting `modern.css`'s override (four tighter fact columns, one clean ~40%-wide trailing gutter), in both light and dark theme, on `/borrowers/B-000008`. Used the CSS-selector overlap between `app.css` and `modern.css` (31 shared selectors, found by diffing selector lists across both files) to triage other candidates for the same defect class rather than guessing screen-by-screen: `.governance__facts` shares the identical auto-fit override, but its `dd` values have no `text-align: end` in `app.css`, so the wider columns just add trailing space rather than a mid-row gap — confirmed harmless live on `/governance` and left unchanged; `.field`, `.field__control`, `.state`, `.drawer`, `.trajectory__*`, `.horizon-control`, `.filter-bar`/`.filter-bar__fields`, and `.pagination` were each diffed property-by-property and found additive/cosmetic (spacing, hit-target sizing, card treatment) rather than layout-breaking, and were left alone. Also spot-checked `/cases`, `/cases/C-018CA12C8A62`, `/simulator/01a05bb1-16b2-72ee-a913-09a8aeb6f4f9` (confirms the interventions-seed fix already landed — applicable interventions now list for `D08LEV`) and the `/admin/jobs` 403 and `/admin/ops` 404 error screens; no further layout defects found on any of them. **Caveat on method:** the Playwright MCP browser in this environment is shared with a concurrent peer session (tabs and the "current tab" pointer shifted mid-investigation without this session navigating them); every screenshot cited above was re-captured in a freshly opened, immediately-used tab after that was discovered, and one earlier capture showing overlapping double-rendered content was identified as a cross-session navigation artifact and discarded rather than reported as a product bug. No test suite exists for this template/CSS pair; the edit only deletes a rule and introduces no new literals, so `tests/unit/test_tokens.py::test_no_design_literal_outside_tokens` was not specifically re-run. No LLM key is required; this is a static CSS fix. | Claude | 2026-09-01 |
